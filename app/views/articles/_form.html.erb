<%= form_for @article, remote: true do |f| %>

  <!-- <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %> -->

  <%= f.text_area :text, onkeydown: 'checkForEntry(this, ' +
      (@article.id ? @article.id.to_s : '"new_article_button"') + ')' %>

  <%= f.text_field :all_tags, placeholder: '#tags',
      id: (@article.id ? 'article_' + @article.id.to_s + '_tags' : 'new_article_tags'),
      onkeydown: 'checkForEntry(this, ' + (@article.id ? @article.id.to_s : '"new_article_button"') + ')',
      autocomplete: :off, autocorrect: :off, autocapitalize: :off, spellcheck: false %>

  <div class="buttons">
    <%= f.submit 'Save', id: @article.id || 'new_article_button',
        class: 'save-button', disabled: true, remote: true %>
    <%= f.submit 'Cancel', class: 'cancel-button',
        type: @article.id ? 'submit' : 'reset',
        onclick: 'document.getElementById("new_article_button").disabled = true' %>
  </div>

<% end %>
