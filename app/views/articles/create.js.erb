var articles = document.getElementById('articles')

var article = "<%=j render partial: 'article', locals: { article: @article } %>";

articles.innerHTML = article + articles.innerHTML;
document.getElementById("new_article").reset();

setTimeout(function () {
  document.getElementById("new_article_button").disabled = true;
}, 0);
