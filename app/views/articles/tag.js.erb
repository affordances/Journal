var tagged_articles_container = document.getElementById('articles');

var tagged_articles = ['<%= raw @articles %>'];

for (var i = 0; i < tagged_articles.length; i++) {
  tagged_articles_container.innerHTML += tagged_articles[i];
}
