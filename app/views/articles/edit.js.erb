var form = "<%=j render partial: 'form', locals: { remote: true } %>";

var elt = document.getElementById('<%= @article.id %>');

elt.outerHTML = form;

var tags = document.getElementById('article_<%= @article.id %>_tags');

allowTagEditing(tags);
