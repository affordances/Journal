var form = "<%=j render 'form' %>";

var elt = document.getElementById('<%= @article.id %>'); //change to getElementById('unique id of article') you have to update the view that renders the article list

elt.outerHTML = form;
