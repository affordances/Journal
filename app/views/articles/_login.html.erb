<%= form_for :session, url: session_path do |form| %>

  <div class='login-header'>
    Sign in
  </div>

  <div>
    <%= form.text_field :email, type: 'email', placeholder: 'Email',
        class: 'sign-in-field' %>
  </div>

  <div>
    <%= form.password_field :password, placeholder: 'Password',
        class: 'sign-in-field' %>
  </div>

  <div>
    <%= form.submit 'Welcome', class: 'sign-in-button' %>
  </div>

  <!-- <div class='password-link'>
    <%= link_to 'Forget your password?', new_password_path,
        onclick: 'login("password-form", "login-form")', remote: true %>
  </div>

  <div>
    <% if Authenticate.configuration.allow_sign_up? %>
      <%= link_to 'Sign up', sign_up_path, class: 'sign-in-button',
          onclick: 'login("signup-form", "login-form")', remote: true %>
    <% end %>
  </div> -->

<% end %>
